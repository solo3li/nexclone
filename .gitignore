# ── Env / secrets — NEVER commit these ────────────────────────
.env
.dev.env
.production.env
.prod.env
.backend.env
.front.env

# ── Database dumps ─────────────────────────────────────────────
data_old_website.sql
migration_new_users.sql
data_db/
railway_dump.sql
dump.sql
prod_backup_thomas.sql

# ── Helm chart lock (regenerated by helm dep update) ───────────
nexclone-chart/Chart.lock
nexclone-chart/charts/*.tgz
